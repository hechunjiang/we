<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    <android.support.v7.widget.RecyclerView
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:id="@+id/rv_transition_file"
        android:layout_width="match_parent"
        android:layout_height="70dp" />

    <RadioGroup
        android:id="@+id/rg_transition_apply"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_weight="1">
        <RadioButton
            android:id="@+id/rb_transition_apply_current"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:drawablePadding="4dp"
            android:padding="10dp"
            android:checked="true"
            android:background="@null"
            android:button="@null"
            android:drawableLeft="@drawable/selector_transition_radio_button"
            android:layout_height="wrap_content"
            android:text="current"
            android:textColor="@color/colorWhite"
            />
        <RadioButton
            android:id="@+id/rb_transition_apply_all"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:drawablePadding="4dp"
            android:padding="10dp"
            android:background="@null"
            android:button="@null"
            android:drawableLeft="@drawable/selector_transition_radio_button"
            android:layout_height="wrap_content"
            android:text="all"
            android:textColor="@color/colorWhite"
            />
    </RadioGroup>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_transition_theme"
        android:layout_width="match_parent"
        android:layout_height="100dp" />

</LinearLayout>